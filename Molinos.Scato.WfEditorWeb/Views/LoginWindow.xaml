<Window x:Class="Molinos.Scato.WfEditorWeb.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Molinos.Scato.WfEditorWeb.Properties"
        MinHeight="100" Width="360" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="100*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="{x:Static properties:Resources.Usuario}" Margin="1" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="1" Name="Usuario" x:FieldModifier="private"/>
        <Label Grid.Row="1" Grid.Column="0" Content="{x:Static properties:Resources.Password}" Margin="1" />
        <PasswordBox Grid.Row="1" Grid.Column="1" Margin="1" Name="Password" x:FieldModifier="private" />
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" FlowDirection="LeftToRight" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static properties:Resources.Ingresar}" Margin="3" Width="100" Click="IngresarClick" IsDefault="True" />
            <Button Content="{x:Static properties:Resources.Cancelar}" Margin="3" Width="100" Click="CancelarClick" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
