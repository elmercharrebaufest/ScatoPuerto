@using Molinos.Scato.Dominio.Recursos
@using Molinos.Scato.Web.Helpers
@{
    ViewBag.Title = Textos.AsignacionDeContingencia_Titulo;
}

<script type="text/javascript">
    urlContingencia = '@Url.Action("ModificarModalidad")';
    urlSinAfip = '@Url.Action("SinAfip")';
    urlSinCupo = '@Url.Action("SinCupo")';
    urlSinFotoCartaPorte = '@Url.Action("SinFotoCartaPorte")';
    urlImprimeCartaPorte = '@Url.Action("ImprimeCartaPorte")';
    urlImprimeTarjetaDeAcceso = '@Url.Action("ImprimeTarjetaDeAcceso")';
    urlEncolaBajaCtgAutomatico = '@Url.Action("EncolaBajaCtgAutomatico")';

    urlTomarFotoCartaDePorteEnCentro = '@Url.Action("TomarFotoCartaDePorteEnCentro")';
    urlDescargaCartaPortePorCtg = '@Url.Action("DescargaCartaPortePorCtg")';
    urlSolicitaConfirmarCTG = '@Url.Action("SolicitaConfirmarCTG")';
    urlValidarCupo = '@Url.Action("ValidarCupo")';

    urlNirsManual = '@Url.Action("NirsManual")';
    urlHumedimetroManual = '@Url.Action("HumedimetroManual")';
    urlBalanzaManual = '@Url.Action("BalanzaManual")';
    urlContingenciaGranos = '@Url.Action("ContingenciaGranos")';
    urlImpresionTicketDeSalida = '@Url.Action("ImpresionTicketDeSalida")';
    urlNoAsignaCalleEnGaritaEntrada = '@Url.Action("NoAsignaCalleEnGaritaEntrada")';

    urlInformarArriboACircularEnCentro = '@Url.Action("InformarArriboACircularEnCentro")';
    urlContingenciaAfipCpe = '@Url.Action("ContingenciaAfipCpe")';
</script>
<script src="@Url.Script("~/Scripts/asignacionDeContingencia.js")" type="text/javascript"></script>

<div class="container-fluid">
    <h3 class="titSecciones">@Textos.AsignacionDeContingencia_Titulo</h3>
</div>

<div class="container-fluid grillaPad">
    <div id="gridContainer">
        @Html.Partial("Listar")
    </div>
</div>
@Html.Partial("Motivo")

<style>
    hr {
        margin: 5px 0;
    }

    .switch {
        position: relative;
        display: inline-block;
        width: 34px;
        height: 18px;
        margin-top: 5px;
    }

        /* Hide default HTML checkbox */
        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

    /* The slider */
    .slider {
        position: absolute;
        cursor: pointer;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: #ccc;
        -webkit-transition: .4s;
        transition: .4s;
    }

        .slider:before {
            position: absolute;
            content: "";
            height: 14px;
            width: 14px;
            left: 4px;
            bottom: 2px;
            background-color: white;
            -webkit-transition: .4s;
            transition: .4s;
        }

    input:checked + .slider {
        background-color: #18a226;
    }

    input:focus + .slider {
        box-shadow: 0 0 1px #2196F3;
    }

    input:checked + .slider:before {
        -webkit-transform: translateX(12px);
        -ms-transform: translateX(12px);
        transform: translateX(12px);
    }

    /* Rounded sliders */
    .slider.round {
        border-radius: 34px;
    }

        .slider.round:before {
            border-radius: 50%;
        }

    h6 {
        margin: 0;
    }
</style>