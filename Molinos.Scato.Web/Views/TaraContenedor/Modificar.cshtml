@using Molinos.Scato.Dominio.Recursos
@model Molinos.Scato.Dominio.Dto.TaraContenedorDto
@{
    Layout = null;
    ViewBag.Title = Textos.TipoComercial_Modificar;
}

@Scripts.Render("~/bundles/jqueryval")

@using (Ajax.BeginForm("Modificar", null, new AjaxOptions(), new { @class = "form-horizontal", data_dialogo_titulo = Textos.TipoComercial_Modificar }))
{
    @Html.ValidationSummary(true)
    @Html.HiddenFor(model => model.Id)
    @Html.Partial("_CrearModificar", Model)
}