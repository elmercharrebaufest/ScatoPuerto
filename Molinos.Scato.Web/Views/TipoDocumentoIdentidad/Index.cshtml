@using Molinos.Scato.Dominio.Recursos
@{
    ViewBag.Title = Textos.TipoDocumentoIdentidad_Titulo;
}

<div class ="container-fluid">
    <div class="titSecciones">@Textos.Administracion_TiposDocumentoIdentidad</div>
</div>

<div class="container-fluid  grillaPad">
    <div class="row">

            @using (Ajax.BeginForm("Index", null, new AjaxOptions { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "gridContainer" }, new { id = "search-form" }))
            {
            
            }
            @Html.ActionLink(Textos.TipoDocumentoIdentidad_Nuevo, "Crear", "TipoDocumentoIdentidad", null, new {@class = "btn btn-primary ajax-editar-link pull-right"})

    </div>
    
    <div id="gridContainer" data-grid-url="@Url.Action("Index")">
        @Html.Partial("Listar")
    </div>
</div>
