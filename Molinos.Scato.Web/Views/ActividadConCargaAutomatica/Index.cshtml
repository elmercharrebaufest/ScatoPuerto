@using Molinos.Scato.Dominio.Recursos
@{
    ViewBag.Title = Textos.ActividadConCargaAutomatica_Titulo;
}

<div class ="container-fluid">
    <div class="titSecciones">@Textos.Administracion_ActividadConCargaAutomatica</div>
</div>

<div class="container-fluid content grillaPad">
    @Html.ValidationSummary(true)
    <div class="row">
        <div class="span12search bordeGris" style="width:auto">
            @using (Ajax.BeginForm("Index", null, new AjaxOptions { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "gridContainer" }, new { id = "search-form", @class = "form-search" }))
            {
                @Html.Label(Textos.Filtrar_Actividades)
                <input type="text" name="filtro" class="input-large"/>
                <button type="submit" class="btn">@Textos.Filtrar</button>               
            }
            </div>
            @Html.ActionLink(Textos.ActividadActividad_Nuevo, "Crear", "ActividadConCargaAutomatica",null, new {@class = "ajax-editar-link btn btn-primary pull-right"})       
    </div>
    <div id="gridContainer">
        @Html.Partial("Listar")
    </div>
</div>